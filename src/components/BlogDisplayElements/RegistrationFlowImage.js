import React from "react"
import image from "src/images/Registration_Flow.png"

export default () => (
  <div style={{ display: "flex", justifyContent: "center" }}>
    <img src={image} />
  </div>
)
